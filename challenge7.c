#include <stdio.h>
#include <stdlib.h>

int main() {
float x,y,z,m;

printf("tapez le premier nombre:");
scanf("%f",&x);

printf("tapez le deuxieme nombre:");
scanf("%f",&y);

printf("taper le troisieme nombre :");
scanf("%f",&z);
m = (((x * 2) + (y * 3) + (z * 5))/10);


printf("la moyenne pondere est %f ",m);



}
